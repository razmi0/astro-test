---
import Spacer from "@/components/ui/Spacer.astro";
import Nav from "../components/Nav";
import { ModeToggle } from "../components/ui/ModeToggle";
import FadeImage from "@/components/ui/FadeImage.astro";
---

<script is:inline>
  const getThemePreference = () => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
  };
  const isDark = getThemePreference() === "dark";
  document.documentElement.classList[isDark ? "add" : "remove"]("dark");

  if (typeof localStorage !== "undefined") {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    });
    observer.observe(document.documentElement, { attributes: true, attributeFilter: ["class"] });
  }
</script>

<header class="flex px-3 items-center justify-center bg-main-400 dark:bg-main-900 w-full fixed z-10 top-0 left-0">
  <!-- <div class="font-bold text-wt-500 text-4xl">LOGO</div> -->

  <FadeImage
    img={{ src: "logo.webp", alt: "colorful logo of the company", class: "border-none" }}
    figure={{ class: "h-20 w-20 border-none rounded-full" }}
  />
  <Spacer />
  <Nav navClassname="h-24 me-4" client:idle />
  <ModeToggle client:idle />
</header>
