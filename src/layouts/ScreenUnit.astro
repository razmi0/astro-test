---
interface Props {
  id: string;
  className?: string;
  children: any;
}

const { id, className } = Astro.props;
---

<section class:list={["min-h-dvh px-12 py-12", [className] || ""]} id={id}>
  <slot />
</section>
