---
import Layout from "@/layouts/Layout.astro";
import "../styles/index.css";
import Header from "@/layouts/Header.astro";
import Footer from "@/layouts/Footer.astro";
import ScreenUnit from "@/layouts/ScreenUnit.astro";
import Hero from "@/components/Hero.astro";
import Content from "@/components/Content/Content.astro";
import ColorTheme from "@/components/ui/Color/ColorTheme.astro";
import { textContent, wrappers, imageTextContent } from "@/shared/data";
---

<Layout title="Accueil">
  <Header slot="header" />
  <Hero slot="hero" />
  <Fragment slot="main" id="main">
    {
      /**
       *
       *
       *
       * Presentation
       *
       *
       *
       */
    }
    <ColorTheme theme="light" curve>
      <ScreenUnit {...wrappers.presentationHeader} />
      <ScreenUnit {...wrappers.presentation}>
        <Content {...textContent.presentationContentOne} />
        <Content {...textContent.presentationContentTwo} />
        <Content {...textContent.presentationContentThree} />
        <Content {...textContent.presentationContentFour} />
      </ScreenUnit>
    </ColorTheme>
    {
      /**
       *
       *
       *
       * Hook
       *
       *
       *
       */
    }
    <ColorTheme theme="dark" curve>
      <ScreenUnit {...wrappers.hookHeaderPrimary} />
      <ScreenUnit {...wrappers.hookPrimary}>
        <Content {...imageTextContent.hookPrimaryTop} />
        <Content {...imageTextContent.hookPrimaryBot} />
      </ScreenUnit>
    </ColorTheme>
    <ColorTheme theme="dark" curve rotate="180deg">
      <ScreenUnit {...wrappers.hookHeaderSecondary} />
      <ScreenUnit {...wrappers.hookSecondary}>
        <Content {...imageTextContent.hookSecondaryTop} />
        <Content {...imageTextContent.hookSecondaryBot} />
      </ScreenUnit>
    </ColorTheme>
    {
      /**
       *
       *
       *
       * Convince ? CTA
       *
       *
       *
       */
    }
    {
      /**
       *
       *
       *
       *
       *
       * Footer
       */
    }
    <ScreenUnit wrapper={{ size: "full" }} theme="dark" />
    <Footer slot="footer" />
  </Fragment>
</Layout>
